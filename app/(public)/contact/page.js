import React from 'react';
import { ContactFormClient } from '@/components/public/contact-form';

export const metadata = {
  title: 'Contact InboxIQ Support | InboxIQ',
  description: 'Reach out to InboxIQ support for account, billing, technical, and general questions.',
};

export default function ContactPage() {
  return <ContactFormClient />;
}
